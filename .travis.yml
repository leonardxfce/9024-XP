os: osx
language: java
script:
  - mvn clean test jacoco:report package sonar:sonar 
jdk:
  - oraclejdk8
sudo: false
addons:
  sonarcloud:
    organization: "leonardxfce-github"
    token:
      secure: $SONAR_TOKEN
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.sonar/cache'
  
